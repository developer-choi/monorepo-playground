import {describe, it} from 'vitest';

describe('Callout', () => {
  describe('General cases', () => {
    // role="note"라 getByRole('note').toHaveClass('custom') (role 노출도 부수 검증)
    it.todo('className을 넘기면 병합된다');
  });
});
