node_modules
dist
.next
build
coverage
.turbo

# docs/는 사람이 직접 작성하는 문서 영역. ESLint/tsc 검사 대상도 아니라
# 빌드·린트와 무관하므로 Stylelint도 적용하지 않는다.
docs

# reset.css는 브라우저 기본 스타일을 무력화하는 baseline. vendor prefix, deprecated 키워드,
# 단일 콜론 pseudo-element 등 의도된 위반이 많아 stylelint 검사 대상에서 제외한다.
packages/design-system/src/styles/reset.css
